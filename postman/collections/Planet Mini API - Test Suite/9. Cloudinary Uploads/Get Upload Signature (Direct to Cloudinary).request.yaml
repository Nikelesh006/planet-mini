$kind: http-request
description: Generates signature for frontend direct-to-Cloudinary image upload.
url: "{{baseUrl}}/api/upload/signature"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{token}}
body:
  type: text
  content: |-
    {
      "folder": "profiles"
    }
order: 1000
